# Exercise 2 — Agent + Task

In Exercise 1, the Agent received a direct instruction.

Now we introduce a **Task**.

Think about the difference:

- Agent = who is doing the work
- Task = what work needs to be done

## Your task

Open `agent_with_task.py` and complete the TODOs.

Then run:

```bash
python agent_with_task.py
```

Observe how the task description and expected output affect the result.
